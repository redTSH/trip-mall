<div class="menu" data-t='{{key}}'>
    {{each items}}
    <dl>
        <dt>{{$value.title}}</dt>
        <dd>
            {{each $value.cities}}
            <a href="{{$value.link}}">{{$value.text}}</a>
            {{/each}}
        </dd>
    </dl>
    {{/each}}
</div>